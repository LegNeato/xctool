// Install to $(DSTROOT)/libexec
INSTALL_PATH = /libexec

// Make sure our product ends up under Build/Products/Debug/libexec/...
DEPLOYMENT_LOCATION = YES
DSTROOT = $(BUILT_PRODUCTS_DIR)
DWARF_DSYM_FOLDER_PATH = $(DSTROOT)$(INSTALL_PATH)
